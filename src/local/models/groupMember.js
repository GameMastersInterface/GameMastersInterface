export default function (id, groupId, characterId) {
  return {
    id: id,
    groupId: groupId,
    characterId: characterId,
    quantity: 1,
    formationX: -1,
    formationY: -1,
    hp: 1,
    maxHP: 1,
  };
}
